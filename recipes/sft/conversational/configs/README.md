# Configuration Examples

The recipe currently accepts typed `chz` command-line overrides. The YAML files
in this directory document the intended P-2 typed configuration shape and are
not yet consumed by `train.py`.
