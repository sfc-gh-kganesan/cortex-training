# Serve a Training Checkpoint

The client can save a weights-only checkpoint and create a separate sampling
job from it. The current Python sequence is documented under
[Start Sampling From A Training Checkpoint](../../reference/cli.md#start-sampling-from-a-training-checkpoint).

A complete guide with endpoint setup, readiness checks, scaling, and teardown
is still planned.
