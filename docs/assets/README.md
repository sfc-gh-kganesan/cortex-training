# Documentation Assets

Store screenshots and redacted output captures in task-specific subdirectories.
Prefer text fixtures for logs and command output when images are not necessary.

Do not commit credentials, account identifiers, unredacted request IDs, or
large generated training artifacts.
